// Title: CamelCase
// Source: Big-O
// Difficulty:
// Time Complexity:
// Topic:

/**
 * Alice đã viết một chuỗi các từ trong CamelCase dưới dạng một chuỗi các chữ cái, s, có các tính chất sau:
 * 
 * Nó là sự ghép nối của một hoặc nhiều từ bao gồm các chữ cái tiếng Anh.
 * Tất cả các chữ cái trong từ đầu tiên đều là chữ thường.
 * Đối với mỗi từ tiếp theo, chữ cái đầu tiên là chữ hoa và các chữ cái còn lại là chữ thường. Cho xâu s, in một dòng duy nhất là số từ
 * trong s.
 *
 * Input:
 * Một dòng duy nhất chứa xâu S.
 * 
 * Constraints
 * 1≤∣s∣≤10^​5
 *
 * Output:
 * Một dòng duy nhất chứa số từ của xâu S.
 *
 * Example 1:
 * Input:
 * saveChangesInTheEditor
 *
 * Output:
 * 5
 **/
